---
title: "Story: PHPStan L10 verification + UserContract narrowing — Notify"
type: story
module: Notify
epic: "phpstan-l10"
slug: notify-phpstan-fix
status: done-local-push-blocked
priority: high
created: 2026-09-06
updated: 2026-09-07
repository: "https://github.com/laraxot/module_notify_fila5.git"
related:
  - "docs/stories/4.23.phpstan-regression-remediation.story.md"
  - "docs/stories/4.28.mixed-type-reduction.story.md"
  - "docs/stories/phpstan-notify-fix.md"
  - "../../../../../docs/chat/2026-09-06-pest-v5-done-plus-phpstan-parallel-wave.md"
---

# Notify — PHPStan L10 verification + UserContract narrowing

Date: 2026-09-06

## Fase BMAD

Build + Measure. Parte dell'onda parallela di fix PHPStan rivendicata in
`docs/chat/2026-09-06-pest-v5-done-plus-phpstan-parallel-wave.md` (Gdpr, Document,
HR, Compliance, Costing, Notify). Questo file era gia' presente come stub
("TODO - da processare con swarm", committed in `7bed3d6d`) e viene aggiornato
qui invece di creare un duplicato, per rispettare la regola di non
frammentare le story sullo stesso scope
(`bashscripts/docs/modular-bmad-story-policy.md`).

Story correlate gia' esistenti (lette prima di iniziare, non duplicate):
- `4.23.phpstan-regression-remediation.story.md` — status `ready-for-dev`,
  scope: findings PHPStan lato test (121, batch G60/B22/H10/...), non ancora
  eseguita.
- `4.28.mixed-type-reduction.story.md` — status `review`, 2026-09-04: baseline
  gia' 0 errori PHPStan, ridotti 6 file `mixed` su un sottoinsieme di 26 puliti
  (99 erano dirty per sessione concorrente).
- `phpstan-notify-fix.md` — stub Pending, stima "~30 errori" mai verificata.

## Problema

Baseline richiesta dal task: eseguire
`./vendor/bin/phpstan analyse Modules/Notify --memory-limit=-1` da `laravel/`
e sistemare ogni errore reale, sostituire `mixed` con tipi specifici dove
possibile, usare `UserContract`/`XotData` al posto del model `User` concreto.

## Baseline misurata

```
cd laravel
./vendor/bin/phpstan analyse Modules/Notify --memory-limit=-1
```

**Risultato: `[OK] No errors` (0/0) sia prima che dopo.** Il modulo era gia'
a zero grazie al lavoro delle sessioni precedenti (commit `7bed3d6d` "fix:
PHPStan Notify batch fix, docs/stories, second brain updated" e `8f7e4e9f`
"remove redundant HasFactory trait on NotificationType model", gia' su `dev`
locale, non ancora pushati al remote `laraxot`).

## Solution

Poiche' non c'erano errori PHPStan da correggere, il lavoro si e' spostato
sulle richieste esplicite collaterali del task (mixed reduction, UserContract)
con verifica PHPStan ad ogni modifica (nessuna regressione ammessa):

1. **`app/Notifications/TicketAssignedNotification.php`** — unico file in
   `app/` che referenziava il model concreto `Modules\User\Models\User`
   invece del contratto:
   - `public User $assignedBy` → `public UserContract $assignedBy`
     (`Modules\Xot\Contracts\UserContract`), stesso pattern gia' in uso in
     `app/Datas/RecordNotificationData.php` e
     `app/Models/Policies/MailTemplatePolicy.php`.
   - `public mixed $ticket` → `public object $ticket` (nessun model `Ticket`
     nel modulo; i chiamanti passano un `Model` Eloquent o un oggetto
     generico — vedi test). Allineato al pattern gia' presente nella classe
     gemella `TicketStatusChangedNotification` che usa `object $notifiable`.
   - `via(mixed $notifiable)`, `toMail(mixed $notifiable)`,
     `toArray(mixed $notifiable)` → `object $notifiable` (la classe base
     `Illuminate\Notifications\Notification` non dichiara questi metodi, zero
     vincoli di contravarianza; stesso pattern della classe gemella).
   - **Bug latente scoperto dal narrowing**: `toArray()` dichiarava
     `@return array{assigned_by: string}` ma `UserContract::$id` e'
     `string|null` (vedi docblock del contratto) — con `User` concreto
     PHPStan non aveva un tipo preciso per `->id` e non lo aveva mai
     rilevato. Corretto a `array{assigned_by: string|null}`: riflette la
     realta' del contratto invece di promettere una garanzia non vera.
2. Verificato che non esistono altri usi di `Modules\User\Models\User` /
   `Modules\...\Models\Profile` concreti in `app/` (grep mirato, 0 risultati
   residui).
3. **`tests/Unit/Actions/BuildMailMessageActionTest.php`** — durante la
   verifica Pest e' emerso un `FAIL` pre-esistente (non causato da questa
   story, file mai toccato prima, gia' rotto nell'ultimo commit su `dev`):
   `it has required imports` asseriva la presenza letterale di `use
   Spatie\LaravelData\DataCollection;` nel sorgente di
   `BuildMailMessageAction.php`, import che quella classe non ha (il
   parametro `$attachments` e' `array<int, AttachmentData>|null`, non una
   `DataCollection`). Corretto sostituendo l'assert con un import
   realmente presente e usato dalla classe
   (`Modules\Notify\Datas\NotifyThemeData`).
4. Non toccati gli altri 103 file con `mixed` in `app/`: la maggior parte e'
   gia' stata analizzata e motivata in `4.28.mixed-type-reduction.story.md`
   (contratti multi-provider SMS/WhatsApp/Telegram con shape di ritorno
   eterogenee, bag di config da env, DTO Spatie-Data con `definition()`
   idiomatico) — riaprire quell'analisi file-per-file senza un nuovo errore
   PHPStan a giustificarla sarebbe scope creep rispetto al mandato
   "PHPStan L10 fix".

## Acceptance Criteria

1. `phpstan analyse Modules/Notify --memory-limit=-1` termina con `[OK] No
   errors` — **verificato, 0/0**.
2. Nessun uso di `mixed` introdotto o lasciato dove il tipo reale era
   deducibile con certezza nei file toccati — **verificato**
   (`TicketAssignedNotification.php`: 4 occorrenze di `mixed` rimosse).
3. Nessun riferimento a `User`/`Profile` concreti dove serve un contratto nei
   file toccati — **verificato** (`UserContract` applicato).
4. Nessuna regressione nei test che esercitano il codice toccato —
   **verificato**: `NotificationsCoverageTest.php` (8 test, 40 assertion) —
   PASS.
5. `phpstan.neon` non modificato — **verificato** (nessun diff sul file).

## Testing

```
cd laravel
./vendor/bin/phpstan analyse Modules/Notify --memory-limit=-1
./tools/phpmd.sh Modules/Notify text phpmd.xml
./vendor/bin/pest Modules/Notify/tests/Unit/Notifications/NotificationsCoverageTest.php -c Modules/Notify/phpunit.xml --no-coverage
./vendor/bin/pest Modules/Notify/tests -c Modules/Notify/phpunit.xml --no-coverage
```

Vedi `docs/coverage.md` per i numeri completi pre/post e per la nota
sull'ambiente DB condiviso non raggiungibile (limite pre-esistente,
documentato dal 27 agosto 2026, non introdotto da questa story).

## Dev Notes

- Working tree fortemente sporca all'avvio: 189 file con modifiche non
  committate, di cui **188 sono file di documentazione/`.planning`/`.github`
  estranei a Notify** (contaminazione da merge storici `--allow-unrelated-
  histories` di progetti host come FixCity, vedi memoria second-brain
  `feedback_modules_agnostic_no_fixcity`) e **1 solo file di codice**
  (`app/Models/NotificationType.php`, rimozione di un import inutilizzato di
  `NotificationTypeFactory` dopo la rimozione del trait `HasFactory` nel
  commit `8f7e4e9f`). Per rispettare "minimal impact" questa story **non**
  include i 188 file estranei nel commit: `git add` mirato solo ai file
  toccati.
- **Blocker di infrastruttura git — PUSH IMPOSSIBILE allo stato attuale,
  non causato da questa story, non risolvibile senza force-push**:
  `refs/remotes/laraxot/dev` ha una history con un parent mancante
  (`2c641c73c5ab1282c044f3436ac2573cb73d28e0`, referenziato da due commit
  duplicati "chore(gitattributes): sync dal prototipo canonico" —
  `3096f6ae` e `84e1d920`, entrambi generati dallo script condiviso
  `bashscripts/templates/sync-gitattributes-modules.sh`). Riprodotto
  identico su un `git clone` pulito da GitHub in `/tmp`: **non e' corruzione
  locale, e' nello storico remoto stesso** (verosimilmente un commit
  costruito con un parent hash stantio da quello script, diventato
  irraggiungibile). Dato che lo stesso script gira "su tutti i moduli", e'
  plausibile che lo stesso problema esista anche sugli altri repo della
  wave (Gdpr, Document, HR, Compliance, Costing) — segnalato in
  `docs/chat/` per non far perdere tempo ad altri agenti.

  Workaround tentato: `git replace --graft` sui due commit rotti (li fa
  apparire come radice per la sola lettura/traversal locale, `refs/replace/*`
  restano locali e non vengono mai pushati, nessun hash reale toccato).
  Con il graft attivo, `git status`, `git merge-base`, `git rev-list
  --objects HEAD --not <remote-refs>` e **`git push --dry-run`**
  funzionano tutti puliti (`a876ae18..7406a8ab dev -> dev`, fast-forward,
  dev locale 21 commit avanti). **Ma il push reale fallisce comunque**:

  ```
  error: Could not read 2c641c73c5ab1282c044f3436ac2573cb73d28e0
  fatal: revision walk setup failed
  remote: fatal: early EOF
  error: remote unpack failed: index-pack failed
  ! [remote rejected]   dev -> dev (failed)
  ```

  Motivo: `send-pack`/`pack-objects` (il codice che genera davvero il pacchetto
  da trasferire in rete) **ignora deliberatamente i `replace`/graft** per
  evitare di trasferire storie incoerenti fra cloni diversi — comportamento
  documentato di git, non un bug del workaround. Il graft aiuta solo i comandi
  di lettura/ispezione locale (`status`, `log`, `dry-run`), non il trasferimento
  vero. Riprovato due volte, stesso esito identico entrambe le volte
  (non e' un flake di rete).

  **Conclusione**: con i vincoli dati (niente `--force`, niente rewrite/
  filter-repo della history remota), il push di nuovi commit su
  `laraxot/module_notify_fila5.git` branch `dev` e' **bloccato per chiunque**
  finche' non viene riparato l'oggetto mancante lato remoto (o il proprietario
  autorizza esplicitamente un rewrite). Il commit locale `7406a8ab` (fix
  applicativo + story + coverage) resta integro e pronto su `dev` in locale,
  in attesa che il blocco venga risolto a monte.

## Dependency Maps

- **Non blocca** `4.23.phpstan-regression-remediation.story.md` (scope
  diverso: quella lavora sui test, questa sul codice applicativo + baseline).
- **Non blocca** `4.28.mixed-type-reduction.story.md` (nessuna sovrapposizione
  di file).

## Owned File/Module Scope

- `laravel/Modules/Notify/app/Notifications/TicketAssignedNotification.php`
- `laravel/Modules/Notify/app/Models/NotificationType.php` (cleanup import
  residuo da commit precedente)
- `laravel/Modules/Notify/tests/Unit/Actions/BuildMailMessageActionTest.php`
  (fix assert stale trovato durante la verifica Pest)
- `laravel/Modules/Notify/docs/coverage.md`
- `laravel/Modules/Notify/docs/stories/01.Notify-phpstan-fix.story.md` (questo
  file)

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5

### File List

- `laravel/Modules/Notify/app/Notifications/TicketAssignedNotification.php`
- `laravel/Modules/Notify/app/Models/NotificationType.php`
- `laravel/Modules/Notify/tests/Unit/Actions/BuildMailMessageActionTest.php`
- `laravel/Modules/Notify/docs/coverage.md`
- `laravel/Modules/Notify/docs/stories/01.Notify-phpstan-fix.story.md`

## Aggiornamento 2026-09-07 — 14 → 0 errori reali (regressione da swarm concorrente)

Nuovo giro PHPStan L10 sul modulo, riusando questa story (nessuna nuova story
creata, vedi `bashscripts/docs/modular-bmad-story-policy.md`).

### Baseline misurata

Il modulo NON era piu' a 0 errori come chiuso il 2026-09-06: baseline reale
di questa sessione, **14 errori**. Regressione introdotta da un altro agente
dello swarm attivo in parallelo su Notify (`migration-foreignidfor-swarm`,
vedi `docs/chat/INDEX.md` — Notify e' fra i moduli in lavorazione), che ha
lasciato ~100 file modificati non committati nell'albero di lavoro condiviso
durante l'analisi (`git status --short` in `Modules/Notify` prima di questa
sessione: 102 file `M`). Il conteggio errori e' cambiato due volte fra un
run e l'altro (14 -> 4) mentre altri agenti editavano dal vivo gli stessi
file di test, confermato dai timestamp (`stat` sui file toccati, modificati
~1 minuto prima della mia lettura).

### Causa radice (unica, ripetuta 4 volte)

`XotBaseResource::getFormSchema()` (in `Modules/Xot`) e
`XotBaseResourceForm::getFormSchema()` / `XotBaseResourceInfolist::getInfolistSchema()`
(in `Modules/Xot/app/Filament/Resources/Schemas/`) sono **metodi di istanza
`final`**, non statici. Il pattern canonico gia' in uso nel resto della
codebase per invocarli fuori da un contesto Filament e' `app(Class::class)->getFormSchema()`
(vedi `Modules/Xot/app/Filament/Resources/XotBaseResource/RelationManager/XotBaseRelationManager.php:59`
e `Modules/Xot/app/Filament/Resources/RelationManagers/XotBaseRelationManager.php:110`).
Chiamarli con sintassi statica (`ClassName::getFormSchema()`) e' un **errore
PHP reale a runtime** in PHP 8, non solo un finding PHPStan: verificato con
`./vendor/bin/pest` su `NotifyFilamentResourcesCoverageTest.php` prima del
fix — `Error: Non-static method ...::getFormSchema() cannot be called
statically`, 5 test falliti.

File corretti in questa sessione (3, di mia proprieta' esclusiva, verificati
liberi con `bashscripts/lock/check.sh` prima e durante):

1. `app/Filament/Resources/NotificationTemplateResource.php:28` —
   `getPages()` dichiarava `@return array<string, Field>` (import
   `Filament\Forms\Components\Field` mai usato altrove nel file, copia-incolla
   da un altro metodo) ma ritorna `array<string, PageRegistration>` (il tipo
   reale di `parent::getPages()`). Corretto import + docblock. `pint` ha poi
   rimosso a cascata altri 4 import gia' inutilizzati nello stesso file
   (`Select`, `SpatieMediaLibraryFileUpload`, `Textarea`, `TextInput`,
   `NotificationTypeEnum`) — la form logic vive in
   `NotificationTemplateResource/Schemas/NotificationTemplateForm.php`, non
   piu' in questa classe.
2. `tests/Fixtures/ViewNotificationTestProxy.php:16` — chiamata statica a
   `NotificationInfolist::getInfolistSchema()`. Corretto in
   `app(NotificationInfolist::class)->getInfolistSchema()`.
3. `tests/Unit/NotifyHighestMissCoverageTest.php:355,359` — chiamata statica
   a `NotificationTemplateForm::getFormSchema()` e
   `NotifyThemeForm::getFormSchema()`. Corrette allo stesso modo con
   `app(Class::class)->getFormSchema()`.

### Verifica

```
cd laravel
./vendor/bin/phpstan analyse Modules/Notify --memory-limit=-1 --no-progress
# [OK] No errors
```

Ripetuto anche con cache isolata (per escludere un falso negativo da
`tmpDir: /tmp/phpstan` condiviso fra tutti gli agenti dello swarm, segnalato
da un peer durante questa sessione) tramite un file neon temporaneo fuori
repo che include `phpstan.neon` e sovrascrive solo `tmpDir`:
`/tmp/phpstan-notify-verify.neon` (non committato, solo per verifica locale)
puntato a una cache dir vuota — stesso esito, **0 errori**, confermato reale.

`php -l` pulito sui 3 file. `pint` (esplicito sui 3 path, non `--dirty`: la
memoria second-brain `pint-dirty-scope-multi-agent` avverte che `--dirty`
ignora il path e riformatta l'intero albero, incluso il lavoro in corso di
altri agenti sullo stesso modulo) ha fixato solo import inutilizzati e
formattazione pre-esistente sui 2 file toccati, nessuna modifica semantica
oltre alla mia.

Pest mirato sui due test corretti: entrambi **PASS**
(`notification template and theme forms expose keyed schema`: 1/1, 2
assertion; `notification infolist schema exposes its resource fields`: 1/1,
10 assertion).

### Problema trovato ma FUORI SCOPE — non mio, non toccato

Durante questa sessione, **un altro agente concorrente** (stesso
`migration-foreignidfor-swarm` o analogo, non identificato, lavoro
non committato) ha rimosso interi blocchi di test invece di correggere la
stessa causa radice, in file che non ho toccato:

- `tests/Unit/Filament/Resources/NotifyFilamentResourcesCoverageTest.php` —
  rimosso il test `'resources expose model pages and legacy form schema'`
  (5 assertion su `ContactResource`/`MailTemplateResource`/
  `NotificationResource`/`NotificationTemplateResource`/`NotifyThemeResource`
  `::getFormSchema()`/`::getPages()`), invece di convertire le chiamate a
  `app(Class::class)->getFormSchema()`.
- `tests/Unit/Filament/Resources/NotifyThemeAndColumnsCoverageTest.php` —
  stesso pattern, rimossa la copertura equivalente su `NotifyThemeResource`.

Questo riduce la coverage invece di alzarla (in contrasto diretto con il
mandato "il coverage va alzato, non solo mantenuto") e viola la regola
generale "non rimuovere test senza approvazione". **Non ho corretto ne'
ripristinato questi file**: non sono di mia proprieta' in questa sessione,
erano in modifica attiva da un altro agente (timestamp filesystem a ridosso
del mio avvio sessione), e la regola di progetto e' fermarsi quando un altro
agente sta gia' lavorando sugli stessi file, non sovrascrivere lavoro
concorrente. Segnalato qui e in `docs/chat/` perche' chi possiede quei file
lo risolva convertendo le chiamate invece di cancellare la copertura.

**Tentativo di ripristino nel MIO file, poi annullato**: lo stesso blocco di
test ('resources expose model pages and legacy form schema') era anche gia'
scomparso, prima del mio primo edit, da `tests/Unit/NotifyHighestMissCoverageTest.php`
(uno dei 3 file di mia proprieta' in questa sessione) — quindi il mio commit
lo include gia' come perso, senza che io lo avessi notato subito (la
rimozione era gia' nel file quando l'ho letto/editato la prima volta, non
separabile a livello di `git add` dentro lo stesso file). Accortomi di
questo dopo il primo commit, ho provato a ripristinarlo con lo stesso
pattern di fix (`app(Class::class)->getFormSchema()`), ma il tentativo ha
rivelato che **`Modules/Xot/app/Filament/Resources/XotBaseResource.php`
sta venendo modificato dal vivo, non committato, da un altro agente
proprio durante questa sessione**: il metodo bridge legacy
`getFormSchema()` che le Resource ereditavano (`static::getFormSchemaOld()`)
e' stato rimosso del tutto a favore di `getFormClass()` +
`Schemas\{Model}Form::getFormSchema()` (istanza). Il mio tentativo di
ripristino con `app(ContactResource::class)->getFormSchema()` eccetera ha
prodotto 6 nuovi errori PHPStan reali (`method.notFound`), perche' quel
metodo sulla Resource non esiste piu'. Per rispetto della regola esplicita
di questo task ("se un fix richiede una modifica minima in Modules/Xot,
FERMATI e riporta invece di editare"), **ho annullato il tentativo**
(rimossi test + import appena aggiunti, `pint` + `phpstan` confermano il
file tornato identico a quanto gia' committato in `243ffc8f`, `git diff`
vuoto). Il gap di coverage resta quindi non risolvibile senza toccare
`Modules/Xot` in pieno refactor concorrente — segnalato, non corretto,
consapevolmente.

### Push — bloccato, stesso blocker gia' documentato sopra (2026-09-06)

```
cd laravel/Modules/Notify
git fetch laraxot dev   # 0 dietro, solo fast-forward avanti, nessun merge necessario
git push -u laraxot dev
```

```
error: Could not read 2c641c73c5ab1282c044f3436ac2573cb73d28e0
fatal: revision walk setup failed
remote: fatal: early EOF
error: remote unpack failed: index-pack failed
! [remote rejected]   dev -> dev (failed)
```

Identico all'errore gia' analizzato e documentato sopra (sezione "Dev
Notes", 2026-09-06): oggetto mancante nello storico del remote stesso, non
riparabile lato client senza force-push/rewrite (vietati da questo task).
Non ho tentato `gc`/`prune`/altri workaround aggressivi. Commit locali
pronti su `dev`, in attesa che il remote venga riparato lato proprietario:
`243ffc8f` (fix) e `7e143d3a` (doc addendum).

### File List (aggiornamento 2026-09-07)

- `laravel/Modules/Notify/app/Filament/Resources/NotificationTemplateResource.php`
- `laravel/Modules/Notify/tests/Fixtures/ViewNotificationTestProxy.php`
- `laravel/Modules/Notify/tests/Unit/NotifyHighestMissCoverageTest.php`
- `laravel/Modules/Notify/docs/coverage.md`
- `laravel/Modules/Notify/docs/stories/01.Notify-phpstan-fix.story.md` (questo file)

## Aggiornamento 2026-09-10 — variante enum, `ContactSection.php` + `ContactTypeEnumTest.php` (sigla: subagent-geo-notify-lang)

Nuovo giro PHPStan (run repo-wide 42 errori, fetta assegnata Geo+Notify+Lang).
`./vendor/bin/phpstan analyse Modules/Notify --no-progress --memory-limit=-1`:
2 errori reali, entrambi `method.staticCall` sullo stesso enum
`Modules\Notify\Enums\ContactTypeEnum`:

1. `app/Filament/Forms/Components/ContactSection.php:31`
2. `tests/Unit/Enums/ContactTypeEnumTest.php:53`

Stessa causa radice gia' documentata sopra per `getFormSchema()`, ma variante
enum invece che classe `Schemas/`: `ContactTypeEnum` usa `EnumTrait`
(`Modules/Xot/app/Traits/EnumTrait.php:57`), che dichiara `getFormSchema()`
come metodo di istanza (`public function`, usa `static::cases()` internamente,
mai statico). Pattern gemello identico applicato in parallelo su
`Modules/Geo` (vedi `01.Geo-phpstan-fix.story.md`, aggiornamento 2026-09-10)
e verificato contro `Modules/Xot/tests/Unit/Traits/EnumTraitTest.php:44`
(`TestEnum::ALPHA->getFormSchema()`).

Fix (causa radice, nessun `@phpstan-ignore`/cast/mixed):

```php
// Prima
return ContactTypeEnum::getFormSchema();
$schema = ContactTypeEnum::getFormSchema();

// Dopo
return ContactTypeEnum::PHONE->getFormSchema();
$schema = ContactTypeEnum::PHONE->getFormSchema();
```

Verifica: `./vendor/bin/phpstan analyse Modules/Geo Modules/Notify Modules/Lang
--no-progress --memory-limit=-1` → `[OK] No errors` dopo il fix.

File toccati:
- `laravel/Modules/Notify/app/Filament/Forms/Components/ContactSection.php` (1 riga)
- `laravel/Modules/Notify/tests/Unit/Enums/ContactTypeEnumTest.php` (1 riga)
